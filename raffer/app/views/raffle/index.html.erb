<!--
<h1>Raffler</h1>

<div ng-controller="RaffleCtrl">
  <form>

  <input type="text" ng-model="newEntry.name">

  </form>
  <ul>
    <li ng-repeat="entry in entries">
      {{entry.name}}
    </li>
  </ul>
</div>
-->
<h1>Raffler</h1>
<div ng-init="entries = [{name: 'Larry'},
{name: 'Curly'},
{name: 'Moe'}]">

<form>
<input type="text" ng-model = "newEntry.name">
</form>
<ul>
  <li ng-repeat="entry in entries">
    {{entry.name}}
  </li>
</ul>
</div>
